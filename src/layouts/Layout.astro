---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
	title: string;
	description?: string;
	image?: string;
    artDate?: string;
    location?: string;
    // パンくず用のデータを受け取る
    breadcrumbs?: { label: string, href: string }[];
}

const { title, description, image, artDate, location, breadcrumbs } = Astro.props;
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, ''); // 末尾のスラッシュを削除

// JSON-LD (パンくずリストの構造化データも追加)
const schemaData = {
  "@context": "https://schema.org",
  "@graph": [
    {
        "@type": "VisualArtwork",
        "name": title,
        "description": description,
        "image": image ? new URL(image, Astro.site).toString() : undefined,
        "dateCreated": artDate,
        "contentLocation": { "@type": "Place", "name": location },
        "creator": { "@type": "Person", "name": "Maestro" }
    },
    // パンくずリストの構造化データ
    breadcrumbs ? {
        "@type": "BreadcrumbList",
        "itemListElement": breadcrumbs.map((crumb, index) => ({
            "@type": "ListItem",
            "position": index + 1,
            "name": crumb.label,
            "item": new URL(crumb.href.replace(/^\//, ''), Astro.site).toString()
        }))
    } : null
  ].filter(Boolean) // nullを除外
};
---

<!doctype html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>✒️</text></svg>" />
		<title>{title} | Digital Old Masters</title>
        <script type="application/ld+json" set:html={JSON.stringify(schemaData)} />
	</head>
	<body>
        <Header />
        
		<main data-pagefind-body>
            {breadcrumbs && (
                <nav class="breadcrumbs" aria-label="Breadcrumb">
                    <ol>
                        {breadcrumbs.map((crumb, index) => (
                            <li>
                                {index === breadcrumbs.length - 1 ? (
                                    // 最後はリンクなし（現在地）
                                    <span aria-current="page">{crumb.label}</span>
                                ) : (
                                    <a href={baseUrl + crumb.href}>{crumb.label}</a>
                                )}
                            </li>
                        ))}
                    </ol>
                </nav>
            )}

			<slot />
		</main>

        <Footer />
	</body>
</html>

<style is:global>
	/* ...以前のスタイルと同じ... */
    :root {
		--c-bg: #F9F7F1;
		--c-text: #333333;
		--c-accent: #CD853F;
        --font-serif: "Times New Roman", "YuMincho", serif;
        --base-url: {import.meta.env.BASE_URL};
	}
	html {
		font-family: var(--font-serif);
		background: var(--c-bg);
		color: var(--c-text);
        line-height: 1.8;
        scroll-behavior: smooth;
	}
    body { margin: 0; padding: 0; }
    main { max-width: 800px; margin: 0 auto; padding: 2rem 1.5rem; min-height: 60vh; }
    a { color: var(--c-accent); text-decoration: none; transition: opacity 0.2s; }
    a:hover { opacity: 0.8; }
    h1, h2, h3 { font-weight: normal; letter-spacing: 0.05em; }
    
    /* パンくずリストのスタイル */
    .breadcrumbs { margin-bottom: 2rem; font-size: 0.85rem; color: #999; }
    .breadcrumbs ol { list-style: none; padding: 0; margin: 0; display: flex; flex-wrap: wrap; }
    .breadcrumbs li:not(:last-child)::after { content: "›"; margin: 0 0.5rem; color: #ccc; }
    .breadcrumbs a { color: #666; }
    .breadcrumbs span[aria-current="page"] { color: #333; }

    /* 記事内のスタイル */
    main img { max-width: 100%; height: auto; display: block; margin: 2.5rem auto; box-shadow: 0 4px 20px rgba(0,0,0,0.08); border-radius: 2px; }
    article h1 { font-size: 2rem; margin-bottom: 0.5rem; text-align: center; }
    .meta-info { text-align: center; color: #666; font-size: 0.9rem; margin-bottom: 2rem; }
    .article-body { font-size: 1.05rem; text-align: justify; }
    .article-body p { margin-bottom: 1.5rem; }
</style>